# Examples

这些示例是公开、安全、脱敏的最小用例，用于验证 Agent 能否正确使用 Memento。

- [[examples/agent-first-run]] — 发给你的 Agent 验收它能否完成 Memento 冷启动。
- [[examples/execution-layer-alignment]] — KB→执行层闭环最小示例：skill/cron 如何声明依赖和验收。

示例文件使用 `status: active`，不是生产规范。真实规则和项目知识应存放在 `规章制度/` 和 `项目知识/` 目录下。
