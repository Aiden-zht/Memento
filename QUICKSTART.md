# Quick Start

**1. Fork 到自己的仓库**

点右上角 Fork。

**2. Clone 到本地**

```bash
git clone https://github.com/你的用户名/memento.git
```

确保你的 Agent 能访问这个目录。

**3. 让 Agent 认识 Memento**

```
读一下 /path/to/memento/AGENTS.md，以后按这里的规范工作
```

**4. 开始使用**

扔文章进去：
```
把这篇文档拖进 inbox/，对 Agent 说：消化 inbox
```

或者创建业务：
```
对 Agent 说：帮我创建一个内容发布写作业务
```

或者建立规范：
```
对 Agent 说：以后发布内容前必须先跑一遍校验
```

**5. 验证**

```bash
cd memento && bash scripts/self-check.sh
```

8/8 = 一切正常。

---

不用学目录结构。不用写 frontmatter。不用管索引。

AI 自己搞定。
