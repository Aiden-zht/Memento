# Quick Start

你只需要做一件事：把文档扔进 `inbox/`。

然后告诉你的 Agent：

```text
这是我的 Memento 仓库：/path/to/memento。请读取 AGENTS.md，以后按这里的规范工作。
```

之后的一切——分类、补 frontmatter、建索引、校验、提交——Agent 自己完成。

---

**日常使用只有三句话：**

```text
消化 inbox
```

```text
以后发布内容前必须先校验
```

```text
帮我创建一个新的内容发布业务包
```

有了想法就对 Agent 说。Agent 会按仓库规范修改知识库、创建业务包、更新相关实现。

---

不用学目录结构。不用写 frontmatter。不用管索引。不用记脚本命令。

AI 自己搞定。