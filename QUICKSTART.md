# Quick Start

**1. Clone 到本地**

```bash
git clone https://github.com/Aiden-zht/Memento.git
cd Memento
```

**2. 让 Agent 认识 Memento**

把你的 Agent 指向这个仓库，让它读取 `AGENTS.md`：

```text
这是我的 Memento 仓库：/path/to/memento。请读取 AGENTS.md，以后按这里的规范工作。
```

**3. 开始使用**

扔文章进去：

```text
把这篇文章拖进 inbox/，对 Agent 说：消化 inbox
```

创建业务包：

```text
对 Agent 说：帮我创建一个新的内容发布业务包
```

建立规范：

```text
对 Agent 说：以后发布内容前必须先跑校验
```

Agent 会自己处理分类、补 frontmatter、建索引、校验和提交。

---

不用学目录结构。不用写 frontmatter。不用管索引。不用记脚本命令。

AI 自己搞定。